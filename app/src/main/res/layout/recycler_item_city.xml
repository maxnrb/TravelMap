<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:padding="16dp">

    <TextView
        android:id="@+id/recycler_item_city_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toStartOf="@+id/recycler_item_city_check_imageView"
        android:layout_marginEnd="16dp"

        android:maxLines="1"
        android:ellipsize="end"

        android:text="@string/recycler_item_first_line_str"
        android:textAppearance="?attr/textAppearanceSubtitle1"
        android:textColor="@color/material_list_item_tint"
        android:textSize="16sp" />

    <ImageView
        android:id="@+id/recycler_item_city_check_imageView"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentEnd="true"
        android:src="@drawable/ic_baseline_check_24"
        android:contentDescription="@string/check_icon_city_recycler_item"
        android:visibility="invisible"/>

</RelativeLayout>